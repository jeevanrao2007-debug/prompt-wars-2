# Frontend deployment config template
# For static frontend hosting, keep this file in repo as documentation.
# If you use a build tool later (Vite/webpack), map this to runtime config.

# Cloud Run backend URL example:
# https://matdata-sahayak-xxxxx-uc.a.run.app
# Leave blank for same-origin deployments.
FRONTEND_API_BASE_URL=
