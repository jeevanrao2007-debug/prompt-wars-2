window.APP_CONFIG = {
    // Leave blank to let app.js fall back to localhost in development and
    // window.location.origin in same-origin production deployments.
    // Cloud Run backend URL example:
    // API_BASE_URL: "https://matdata-sahayak-xxxxx-uc.a.run.app",
    API_BASE_URL: "",
};
